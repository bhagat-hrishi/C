#include<stdio.h>
int y;
int y=90;

//int m=9;//error
//int m=3;//error
int main()
{
    int x;
    int x = 5;
    printf("%d", x);
    return 0;
}
/*
C allows a global variable to be declared again
 when first declaration doesn’t initialize the variable.
*/

//but all above is wrong in c++
